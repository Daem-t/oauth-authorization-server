spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oauth2_dev
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  
  mail:
    host: smtp.qq.com
    port: 465
    username: 945460266@qq.com
    password: 'phglrqxfjvqabdjf'
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  
  application:
    name: oauth-authorization-server-test

logging:
  level:
    org.springframework: warn
    com.daem.oauth: info
    com.icegreen.greenmail: warn

app:
  base-url: http://localhost:8080
  auth:
    default-role: USER